const Employee = require('../lib/Employee');
const Intern = require('../lib/Intern');

test('creates an Inten object', () => {
    const intern = new Intern('Intern1');

    expect(tyoeof(intern)).toBe();
});